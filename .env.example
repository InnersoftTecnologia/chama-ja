# Banco (MariaDB)
DB_HOST=localhost
DB_PORT=3306
DB_USER=mysql
DB_PASSWORD=mysql
DB_NAME=chamador

# Edge API
EDGE_HOST=0.0.0.0
EDGE_PORT=7071
EDGE_DEVICE_TOKEN=dev-edge-token
EDGE_TENANT_CPF_CNPJ=

# JWT (produção: use valor seguro)
JWT_SECRET=dev-jwt-secret-change-me
JWT_ISSUER=chamador-edge
JWT_TTL_MINUTES=720
